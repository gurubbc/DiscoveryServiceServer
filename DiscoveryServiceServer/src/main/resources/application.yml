#configure this Discovery server
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
 
 
#HTTP (tomcat) port no
server:
  port: 4567